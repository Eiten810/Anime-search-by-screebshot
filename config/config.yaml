seed: 42

device: auto

data:
  processed_dir: data/processed
  splits_dir: data/splits

embeddings:
  gemini_original: embeddings/gemini_original
  gemini_original_train: embeddings/gemini_original_train

  gemini_512: embeddings/gemini_512
  gemini_512_train: embeddings/gemini_512_train

  resnet50: embeddings/resnet50
  resnet50_train: embeddings/resnet50_train

  resnet50_triplet: embeddings/resnet50_triplet
  resnet50_triplet_train: embeddings/resnet50_triplet_train

models:
  classifier: models/best_resnet50.pth
  triplet: models/best_triplet_resnet50.pth

search:
  top_k: 10

evaluation:
  top_k: 10

paths:
  logs: logs
  results: results